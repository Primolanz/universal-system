import{STORE_CONFIG}from'../config/store';export const formatCurrency=v=>new Intl.NumberFormat(STORE_CONFIG.locale,{style:'currency',currency:STORE_CONFIG.currency}).format(Number(v||0));export const slugify=v=>v.normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().trim().replace(/[^a-z0-9]+/g,'-').replace(/(^-|-$)/g,'');export const friendlyError=e=>e?.message?.includes('duplicate')?'Já existe um cadastro com estes dados.':e?.message||'Não foi possível concluir a operação. Tente novamente.'